# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env files (contain config keys) — never commit
.env
.env.*

# Internal working docs / deploy helpers — keep local only
PROGRESS.md
BUILD_PROMPT.md
ROADMAP.md
SCALING.md
SETUP.md
supabase/DEPLOY_ALL.sql

# One-off account-migration scripts (Aug 2026) — kept local, not part of the app
deploy_full_rebuild.sh
migrate_data.py
migrate_storage.py

# Local agent tooling / installed skills — keep local only
.claude/
.agents/
skills-lock.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
